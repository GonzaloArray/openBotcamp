const altura = 180;
const alturaMetros = 1.80;
const peso = 80.0;
const alturaRedondeada = Math.ceil(179.7);
const pesoRedondeada = Math.floor(80.2);
const maximoValor = Number.MAX_VALUE + 1 === Number.MAX_VALUE;


console.log(
        altura,
        alturaMetros,
        peso,
        alturaRedondeada,
        pesoRedondeada,
        maximoValor
    );